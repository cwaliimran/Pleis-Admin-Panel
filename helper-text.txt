// ------------- EURO SIGN -------------
€

// ------------- DESCRIPTION -------------
<TableCell className="text-left">
    <TruncatedTextWithModal text={item?.description} title="Description" />
</TableCell>


// ------------- DATE FORMATTER -------------
<TableCell className="text-left">{fDate(item?.createdAt, formatStr.split.date)}</TableCell>


// ------------- GET ORGANIZATION & ORGANIZATION IDS - ORGANIZER ONLY -------------
const { organizerOrganizations, organizerOrganizationIds } = useCompanySelection();


// ------------- GET COMPANY ID & ORGANIZATION ID - ADMIN ONLY -------------
const { companyId, organizationId } = useCompanySelectionState();