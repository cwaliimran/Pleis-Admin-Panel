import { OrderAcceptanceSettings } from './types';

/** Used for an organization that has never saved ordering settings. */
export const DEFAULT_ORDER_ACCEPTANCE_SETTINGS: OrderAcceptanceSettings = {
  automaticOrderAcceptance: true,
};
